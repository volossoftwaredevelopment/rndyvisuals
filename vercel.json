{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "rewrites": [
    { "source": "/admin", "destination": "/admin.html" },
    { "source": "/admin/login", "destination": "/admin-login.html" }
  ],
  "redirects": [
    { "source": "/admin.html", "destination": "/admin", "permanent": false },
    { "source": "/admin-login.html", "destination": "/admin/login", "permanent": false }
  ],
  "headers": [
    {
      "comment": "Everything under /assets carries a content hash in its filename, so a changed file is a changed URL. Without this the browser revalidated every stylesheet, script and font on every single page change — seven round trips per click, and the text re-flowed when the fonts finally landed.",
      "source": "/assets/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    },
    {
      "comment": "Not hashed — cache for a day so a replaced file still appears without waiting a year.",
      "source": "/media/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=86400" }]
    }
  ]
}
